{
  "name": "vizengine",
  "version": "0.1.0",
  "description": "Zero-dependency browser visualize engine — markdown-superset format with ASCII-theme diagrams",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test"
  },
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
